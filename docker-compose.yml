services:
  forum:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      NODE_ENV: production
    volumes:
      - forum-data:/app/data
    restart: unless-stopped

volumes:
  forum-data:
